events {
    worker_connections  1024;
}

http {
    upstream app {
        server web1.localdomain.lan;
        server web2.localdomain.lan;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app;
        }
    }
}
